## How to Deploy
1. cd /dist/<project>/browser/
2. git init
3. git add .
4. git commit -m "Deploy"
5. git branch -M gh-pages
6. git remote add origin https://github.com/githubrepo
7. git push -f origin gh-pages

8. in Github -> theRepo -> Settings -> Pages -> From Branch -> Save
